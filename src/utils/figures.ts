import chalk from 'chalk';
import figureSet from 'figures';

export const iconSuccess = chalk.green(figureSet.tick) + ' ';
export const iconInfo = chalk.cyan(figureSet.info) + ' ';
export const iconWarning = chalk.yellow(figureSet.warning) + ' ';
export const iconError = chalk.red(figureSet.cross) + ' ';
